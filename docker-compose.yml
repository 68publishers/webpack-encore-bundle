services:
    php83:
        build:
            context: .
            dockerfile: Dockerfile
            target: php83
        container_name: 68publishers.webpack-encore-bundle.83
        volumes:
            - .:/var/www/html:cached
